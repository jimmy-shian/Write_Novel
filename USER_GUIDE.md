# 📖 AI Novel Factory - 使用者操作指南 (v4.0.0)

歡迎使用 **AI Novel Factory (AI 小說工廠)**！這是一個專門為小說家、編劇和創意工作者打造的長篇小說智慧創作工作台。

系統整合了專業的多代理協作（Multi-Agent System）、Graphiti 時序動態記憶圖譜與 OpenDesign 極簡暗黑工作台，支援桌面 4 欄式 IDE 工作區與手機行動端 RWD 響應式佈局。

---

## 🖥️ 1. 介面佈局導覽

工作台採用專業開發者等級的極簡高對比設計，完全去除干擾創作的卡通 Emoji，劃分為五大清晰工作區：

```
┌────┬──────────────┬────────────────────────────────┬──────────────┐
│ 活 │   作品導航   │          中央創作主畫布         │  AI 導演總控 │
│ 動 │   (目錄樹)   │       (正文 / Diff / 圖譜)     │    (Copilot) │
│ 列 │              │                                │              │
│    │  · 作品切換  │  · 章節正文即時編輯            │  · 階段選擇  │
│ 48 │  · 章節目錄  │  · 審閱修訂對比 (Diff Viewer)  │  · 自主寫作  │
│ px │  · 增減章節  │  · 時序記憶圖譜 (Graphiti)     │  · 推理串流  │
├────┴──────────────┴────────────────────────────────┴──────────────┤
│ 底部工作列 (Bottom Dock)：即時推理與生成日誌 / 自主寫作進度 (可收合)│
└──────────────────────────────────────────────────────────────────┘
```

### 桌面端 5 大區域
1. **左側活動列 (Activity Rail, 48px)**：
   - **章節編輯器**：切換回中央正文編寫畫布。
   - **審閱對比 (Diff)**：進入版本修改差異審閱與套用介面。
   - **時序記憶圖譜 (Graphiti)**：開啟時序事實與實體檢視器。
   - **專用術語庫 (Glossary)**：管理小說專有詞彙與約束規範。
   - **系統設定**：配置 LLM API 金鑰、動態選擇模型與推理參數。
2. **作品導航抽屜 (Explorer Drawer, 260px)**：
   - 上方為作品切換選單與「新增小說」功能。
   - 中間為章節目錄樹，顯示各章索引與字數統計。
   - 提供「新章」快速新增按鈕。
3. **中央創作主畫布 (Workspace Canvas)**：
   - 頂部工作列：顯示作品名、當前章節、同步狀態指示點（綠色已同步 / 紅色未儲存 / 黃色儲存中）。
   - 模式切換鈕：快速在「編輯器」、「審閱對比」、「記憶圖譜」間切換。
   - 正文編輯畫布：支援一鍵複製與快捷鍵操作。
4. **AI 導演總控室 (Copilot Drawer, 320px)**：
   - 階段派發器：世界觀、角色聖經、篇卷骨架、正文撰寫、審閱修訂、深度評估。
   - 全自動自主寫作卡片：一鍵啟動或暫停後端無人值守寫作管線。
   - 思考推理歷程（Thinking Process Stream）即時展開展示。
5. **底部收合抽屜 (Bottom Dock)**：
   - 記錄底層 SSE 傳輸、各階段狀態跳轉與自癒日誌。

### 行動端模式 (< 768px / Android APK)
當螢幕寬度小於 768px 或於 Android 裝置運行時，系統自動適配為行動專屬模式：
- **底部 3 按鈕導航列**：提供「目錄」、「正文」、「導演」快速單手切換。
- **平滑覆蓋抽屜**：點擊「目錄」從左側滑出導航抽屜；點擊「導演」從右側滑出控制面板；點選遮罩層即可返回編輯正文。

---

## ✍️ 2. 三種創作模式指南

系統支援三種深淺不同的創作互動型態：

### 模式 A：純手動寫作 (Manual Writing)
適合您完全由自主靈感書寫正文：
1. 在左側目錄選取章節，中央畫布即時載入該章內容。
2. 在編輯區輸入文字，頂部即時更新字數與行數。
3. 當有新輸入時，狀態指示點會顯示紅色「未儲存」，按下鍵盤 **`Ctrl + S`**（或點擊頂部「儲存」按鈕）即可立即寫入資料庫。
4. 點擊頂部工具列「**一鍵複製**」，立即將全章文字複製至剪貼簿，並顯示視覺打勾確認。

### 模式 B：人機協同審閱 (Collaborative Proposal & Diff Review)
適合讓 AI 擔任您的專業文學責任編輯：
1. 在右側 AI 導演面板選擇「**審閱修訂**」階段，並可在輸入框輸入具體要求（例如：「強化動作場面的緊湊度，並檢查招式描寫」），點擊「執行階段生成」。
2. AI 會針對當前章節正文產出修改建議，並自動登記於「**草稿建議收件箱**」。
3. 點擊「**審閱對比 (Diff)**」，系統將以 LCS 行級差異演算法展示：
   - **綠色底線 (`+`)**：AI 建議新增的描寫。
   - **紅色刪除線 (`-`)**：AI 建議刪除或替換的贅字。
4. 檢閱無誤後，點擊「**一鍵套用**」，新文字將直接覆蓋至正文畫布並更新資料庫；若不滿意則點擊「**放棄建議**」。

### 模式 C：全自動自主寫作 (Autonomous Pipeline)
適合百萬字長篇小說的無人值守結構化推進：
1. 點擊頂部或導演抽屜中的「**自主寫作**」按鈕。
2. 後端大腦將自驅啟動無人值守引擎：
   - 自動檢驗當前小說進度。
   - 若無世界觀 ➔ 自動構建世界觀設定。
   - 若無角色 ➔ 自動刻畫主要角色聖經。
   - 若無大綱 ➔ 自動排布 10~20 卷宏觀骨架與細部大綱。
   - 逐章撰寫正文，並自動登錄時序記憶圖譜事實。
3. 您可以隨時關閉瀏覽器，後端背景持續進行；再次開啟工作台時，系統自動向後端輪詢最新進度。
4. 點擊「**停止自主**」可隨時安全中斷管線。

---

## 🧠 3. Graphiti 時序記憶圖譜使用指南

傳統 AI 寫作受限於固定上下文窗口（Sliding Window），寫到後期容易遺忘前期設定，或對已經改變的事實產生矛盾（例如已亡故的角色再次現身）。**AI Novel Factory 導入 Graphiti 時序知識圖譜**，以動態切片方式解決此問題。

### 時序切片與事實狀態
點擊左側活動列的「**時序記憶圖譜**」，您將看到：
- **當前章節切片**：系統只展示在當前章節屬於**有效狀態**的事實。
- **事實有效期 (Valid Range)**：標記 `第 X 章起有效` 或 `第 X 章 ~ 第 Y 章`。
- **事實作廢 (Invalidation)**：若某一事實因情節反轉失效（例如「林霄修為是築基初期」在第 15 章被突破），點擊該事實旁的「**作廢**」，輸入作廢原因（例如「在天斷崖突破至築基中期」），系統即將其作廢，防止後續章節生成時產生錯誤引用。
- **實體庫 (Entities)**：追蹤登場角色、神兵法寶、宗門勢力與修煉法則之初登場章節與最後更新章節。

### 本章事實自動提取
在撰寫或生成完某一章正文後：
1. 進入時序圖譜頁面。
2. 點擊右上角「**本章事實自動提取**」。
3. 後端會自動呼叫實體抽取器，將本章發生的關鍵事件、人物修為變動與法寶得失自動提煉為結構化事實並寫入圖譜。

---

## 🏷️ 4. 故事專用術語庫 (Glossary Constraints)

長篇小說常具備特有的世界觀名詞（例如宗門名稱、功法招式、境界劃分）。本系統將術語庫提升為**後端 Prompt 強制約束機制**。

### 建立與管理術語
1. 點擊左側活動列的「**故事專用術語庫**」圖標。
2. 點擊「**新增術語**」，填寫以下欄位：
   - **術語名稱**：例如「太上玄冥功」。
   - **所屬分類**：通用術語、修煉體系、地理名詞、功法法寶、宗門勢力。
   - **定義與嚴格約束規範**：例如「僅魔門太上長老可修煉，施展時伴隨黑炎，不可與正道玄光共存」。
3. 點擊「**儲存術語**」。

### 運作原理
後端在執行世界觀、角色、大綱、正文寫作與審閱時，會自動遍歷並將此術語表注入各 Agent 的系統提示詞約束中，確保生成文本中的名詞用法與定義 100% 精準一致。

---

## ⚙️ 5. 系統與 LLM 參數設定

點擊活動列底部的「**系統設定**」齒輪按鈕：

1. **單一事實來源版本資訊**：
   - 頂部展示當前系統版本（例如 `AI Novel Factory v4.0.0 (ObsidianGraphiti)`），點擊卡片可一鍵複製版本號。
2. **API Base URL**：輸入相容於 OpenAI 規範之端點網址（例如 NVIDIA NIM、OpenAI 或本地 Ollama）。
3. **API Key**：輸入您所持有的 API 金鑰。
4. **動態讀取可用模型**：
   - 點擊「**動態讀取可用模型**」，系統向端點查詢並即時渲染為簡潔的標籤選擇片（Model Chips）。
   - 點選標籤即可快速選用，亦可手動輸入自訂模型名稱。
5. **多樣性溫度 (Temperature)**：拖曳滑桿調整生成隨機性（0.0 ~ 1.5）。
6. **啟用模型深度推理 (Thinking Stream)**：勾選啟用支援思考模式的模型（如 DeepSeek-R1, Nemotron 等）即時推理輸出。
7. 點擊「**儲存變更**」完成持久化。

---

## 📦 6. 小說全書匯出

若需將作品匯出至本機，可透過後端匯出 API 或 CLI 取得完整成品：
- **純文字 TXT (`format=txt`)**：章節標題與正文排列整齊，適合導入各類電子書閱讀器。
- **結構化 Markdown (`format=markdown`)**：保留作品標題、世界觀、角色卡片、大綱設定與章節正文。
- **便攜離線 HTML 閱讀器 (`format=html`)**：內建極簡夜間閱讀主題，單檔即可在任何離線瀏覽器中流暢閱讀全書。
